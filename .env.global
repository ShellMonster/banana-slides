# ============================================================================
# Banana Slides 国外官方镜像源配置
# ============================================================================
#
# 使用方法：
#   1. 自动配置（推荐）：运行 bash setup-mirrors.sh
#   2. 手动配置：复制此文件为 .env.detected
#
# ============================================================================

# 检测到的地区
DETECTED_REGION=GLOBAL

# Debian apt 镜像源（官方）
# 用于安装 curl 等系统依赖
APT_MIRROR=deb.debian.org

# GitHub Container Registry（官方）
# 用于获取 uv 工具
UV_IMAGE=ghcr.io/astral-sh/uv:latest

# Python PyPI 镜像源（官方）
# 用于 uv sync 安装 Python 依赖
PYPI_MIRROR=https://pypi.org/simple/

# npm 镜像源（官方）
# 用于 npm install 安装前端依赖
NPM_MIRROR=https://registry.npmjs.org/
